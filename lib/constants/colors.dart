// google_font = Protest Revolution
import 'package:flutter/material.dart';

// Primary Color - Orange
const Color primaryColor = Color(
    0xFFFF6F00); // Vibrant Orange (Main color for buttons, highlights, etc.)

// Danger - Red
const Color dangerColor = Color(
    0xFFFF3B30); // Bright Red (For urgent messages, danger, notifications)

// Background Color - White
const Color backgroundColor =
    Color(0xFFF5F5F5); // White (Clean and neutral background)

// Font Colors
const Color textColor =
    Color(0xFF333333); // Dark Gray for standard text (Readable and clean)
const Color whiteTextColor = Color(
    0xFFFFFFFF); // White text (for contrast on darker elements like buttons)

// Icon Colors
const Color iconColor = Color(0xFF333333); // Dark Gray for inactive icons
const Color activeIconColor =
    Color(0xFFFF6F00); // Orange for active icons (to match buttons)
