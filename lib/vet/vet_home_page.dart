import 'package:flutter/material.dart';

class VetHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Vet Dashboard")),
      body: Center(child: Text("Welcome to Vet Home Page")),
    );
  }
}
